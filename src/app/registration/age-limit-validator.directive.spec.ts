import { AgeLimitValidatorDirective } from './age-limit-validator.directive';

describe('AgeLimitValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AgeLimitValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
