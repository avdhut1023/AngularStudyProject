import { CustomBlankDirective } from './custom-blank.directive';

describe('CustomBlankDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomBlankDirective();
    expect(directive).toBeTruthy();
  });
});
